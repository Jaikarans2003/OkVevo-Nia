# Public pack on push to staging. Feed: https://releases.okvevo.com/staging/
# Secrets: STAGING_* + RELEASES_S3_*. CSC_*/WIN_CSC_*/APPLE_API_* optional until certs exist.
# No GitHub Environments.

name: Desktop staging

on:
  push:
    branches: [staging]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: desktop-staging-${{ github.ref }}
  cancel-in-progress: false

jobs:
  pack:
    uses: ./.github/workflows/desktop-pack.yml
    with:
      pack_target: staging
      feed_prefix: staging
      yml_mode: staging
      allow_unsigned: true
    secrets: inherit
